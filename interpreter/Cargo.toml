[package]
name = "eva"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["dylib", "staticlib"]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"
debug = false
strip = true

[features]
default = []
